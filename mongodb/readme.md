# Create podman container and run it

```zshc
podman create --name mongo -it mongo
podman start mongo
```

# Results of the practical task HBase:

The schreenshots with results in this repo under /screenshots folder